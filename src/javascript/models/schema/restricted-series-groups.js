const restrictedSeriesGroups = {
  'expires-after': '',
  'restricted-series': {
    jsonApi: 'hasOne',
    type: 'series',
  },
  'restricted-group': {
    jsonApi: 'hasOne',
    type: 'groups',
  },
}

export default restrictedSeriesGroups

import React from 'react'

const ProgrammeAutosuggestMock = ({ programme }) => {

  return <div>
    Need to mock this component - ideally taking a `programme` as prop so simulating a click -> returns equivalent of onSuggestionSelected(e, props.programme)
  </div>
}

export default ProgrammeAutosuggestMock
